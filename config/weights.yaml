# Scoring weights for the REDROB composite scoring engine.
# Only weights and multipliers live here. No logic.
#
# Fallback #2: location added as a new scoring component (0.05).
# Fallback #10: education reduced from 0.10 → 0.05 (education is a tiebreaker).
# The 6 component weights sum to exactly 1.0.

title: 0.25
skills: 0.30
experience: 0.25
education: 0.05
behavior: 0.10
location: 0.05

penalty_multiplier: 1.0
honeypot_multiplier: 1.0
